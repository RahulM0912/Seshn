// Single place for the support contact — footer, FAQ, and settings all link
// here, so a future switch to a support@ address is a one-line change.
export const SUPPORT_EMAIL = "seshn.in126@gmail.com";
export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}?subject=${encodeURIComponent("Seshn — question")}`;
